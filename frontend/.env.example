# Base URL of the backend API.
# Copy this file to .env (or set the variable in your host, e.g. Vercel) and adjust.
# When unset, the app falls back to the production Render URL defined in src/config.js.
VITE_API_URL=http://localhost:8000
